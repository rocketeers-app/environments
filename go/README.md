# Go

Minimal Go app (standard library `net/http` only) for the Rocketeers **Go** environment.

- Build: `go mod download && go build -o bin/app .`
- Run: `PORT=8080 ./bin/app` (listens on `127.0.0.1:$PORT`)
